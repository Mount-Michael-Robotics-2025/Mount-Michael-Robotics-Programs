#include "vex.h"

using namespace vex;

namespace brainArt {
    class buttonHorizontal {
        public:
            int xPosition;
            int yPosition;
            int length;
            int radius;
            void press();

            buttonHorizontal(int _xPosition)
    };
}